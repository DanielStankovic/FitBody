<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    
    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorPrimaryDark</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="tabSelectedTextColor">@color/selected_tab_color</item>
        <item name="android:elevation">6dp</item>

    </style>
    
    <style name="AppTabTextAppearance" parent="AppTabLayout">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/tabText</item>
        <item name="android:textAllCaps">true</item>
        
    </style>

    <style name="VerticalScrollBarStyle">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_vertical_thumb</item>
        <item name="android:scrollbarTrackVertical">@drawable/srollbar_vertical_track</item>
        <item name="android:scrollbarSize">6dp</item>
        <item name="android:fadeDuration">2000</item>
        <item name="android:scrollbarDefaultDelayBeforeFade">1000</item>


    </style>

</resources>
